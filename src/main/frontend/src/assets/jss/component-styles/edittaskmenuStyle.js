import { iconSize } from '../styleConstants';

const edittaskmenuStyle = {
  optionsIcon: {
    ...iconSize,
    marginLeft: 'auto',
    display: 'block',
    padding: '1px 10px 0'
  }
};

export default edittaskmenuStyle;
